@using X.PagedList.Mvc;
@model X.PagedList.PagedList<LabExameWebsite.Models.TipoExame>
@{ViewBag.Title = "Lista Tipos de Exame";}

<div class="titulo">
    <h2 class="texto-titulo">TIPOS DE EXAME</h2>
    <hr class="divisor-subtitulo" />
    <p class="subtitulo">Lista onde é possível ações para cada item. Abaixo cadastre um novo Tipo de Exame.</p>
</div>

<div class="painel-custom">
    <div class="row lista-propriedades">
        <div class="col-md-4"><b>Tipo de Exame</b></div>
        <div class="col-md-5"><b>Descrição</b></div>
        <div class="col-md-3"><b>Ações</b></div>
    </div>

    <hr class="linha-tabela-customizada" />

    @foreach (var item in Model)
    {
        <div class="row">
            <div class="col-md-4"><b>@Html.DisplayFor(modelItem => item.NomeTipoExame)</b></div>
            <div class="col-md-5"><b>@Html.DisplayFor(modelItem => item.DescricaoTipoExame)</b></div>
            <div class="col-md-3">
                <a href="@Url.Action("Edit", "TipoExame", new { id = item.TipoExameID })" class="btn btn-sm btn-success form-group"><i class="fa fa-check-square-o"></i> Editar</a>
                <a href="@Url.Action("Details", "TipoExame", new { id = item.TipoExameID })" class="btn btn-sm btn-primary form-group"><i class="fa fa-info"></i> Detalhes</a>
                <a href="@Url.Action("Delete", "TipoExame", new { id = item.TipoExameID })" class="btn btn-sm btn-danger  form-group"><i class="fa fa-trash"></i> Excluir</a>
            </div>
        </div>
        <hr />
    }

    <div>
        <a href="@Url.Action("Create", "TipoExame")" class="btn btn-success form-group"><i class="fa fa-plus"></i> Cadastrar Novo</a>
    </div>

</div>
<div class="row text-center">
    @Html.PagedListPager(Model, pagina => Url.Action("Index", new { pagina }), new PagedListRenderOptions { LinkToFirstPageFormat = "<<", LinkToPreviousPageFormat = "<", LinkToNextPageFormat = ">", LinkToLastPageFormat = ">>", MaximumPageNumbersToDisplay = 4 })
</div>